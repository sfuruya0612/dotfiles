---
- name: Create alacritty config directory
  file:
    path: "{{ ansible_env.HOME }}/.config/alacritty"
    state: directory

- name: Symbolic link ~/dotfiles/roles/alacritty/files/alacritty.yml -> ~/.config/alacritty/alacritty.yml
  file:
    src: "{{ ansible_env.HOME }}/dotfiles/roles/alacritty/files/alacritty.yml"
    dest: "{{ ansible_env.HOME }}/.config/alacritty/alacritty.yml"
    state: link
    force: yes
